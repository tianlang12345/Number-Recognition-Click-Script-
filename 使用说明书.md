# ScreenMonitor.ahk 使用说明书

## 一、必要前置

1. **AutoHotkey v2.0**
   - 必须安装 AutoHotkey v2.0（不是 v1.1）。
   - 下载地址：https://www.autohotkey.com/v2/
   - 安装后双击 `ScreenMonitor.ahk` 即可运行。

2. **操作系统**
   - Windows 10 或 Windows 11。
   - 脚本已设置 DPI 感知，坐标按物理像素计算。

3. **OCR 引擎（二选一）**
   - **PowerShell OCR（默认）**：依赖 Windows 内置 OCR 引擎，需 PowerShell 5.1+。
   - **Tesseract OCR（推荐，识别率更高）**：
     - 下载并安装 Tesseract：https://github.com/UB-Mannheim/tesseract/wiki
     - 默认安装路径：`C:\Program Files\Tesseract-OCR\tesseract.exe`
     - 在 `ScreenMonitorConfig.ini` 中设置：
       ```ini
       OCRMethod=tesseract
       TesseractPath=C:\Program Files\Tesseract-OCR\tesseract.exe
       ```

4. **屏幕可见性**
   - 被监控区域必须可见，不能被其他窗口完全遮挡，否则 OCR 无法识别。

---

## 二、配置文件

配置文件与脚本同目录，文件名为：`ScreenMonitorConfig.ini`。

首次运行脚本时若不存在该文件，会自动生成一个默认配置。

### [Settings] 全局设置

| 键名 | 说明 | 默认值 |
|------|------|--------|
| `Enabled` | 启动后是否立即开始监控：`1`=是，`0`=否 | `1` |
| `Interval` | 监控循环间隔，单位毫秒 | `1000` |
| `OCRLanguage` | PowerShell OCR 语言，如 `zh-Hans`、`eng` | `zh-Hans` |
| `OCRMethod` | OCR 方式：`powershell` 或 `tesseract` | `powershell` |
| `TesseractPath` | Tesseract 可执行文件路径 | `C:\Program Files\Tesseract-OCR\tesseract.exe` |
| `LoopClickEnabled` | 启动后是否启用循环点击：`1`=是，`0`=否 | `0` |
| `LoopClickX` | 循环点击的 X 坐标 | `500` |
| `LoopClickY` | 循环点击的 Y 坐标 | `500` |
| `LoopClickInterval` | 循环点击间隔，单位毫秒 | `11000` |
| `LoopClickButton` | 循环点击的按键：`L`、`R`、`M` | `L` |
| `ClickSequenceDelay` | 全局默认点击序列间隔，单位毫秒 | `50` |

### [ZoneN] 监控区域设置

每个 `[ZoneN]` 表示一个独立的监控区域。

| 键名 | 说明 | 示例 |
|------|------|------|
| `Name` | 区域名称 | `Zone1` |
| `X` | 区域左上角 X 坐标 | `100` |
| `Y` | 区域左上角 Y 坐标 | `200` |
| `W` | 区域宽度 | `120` |
| `H` | 区域高度 | `40` |
| `Threshold` | 阈值 | `50` |
| `Operator` | 比较运算符：`<`、`>`、`<=`、`>=`、`=` | `<` |
| `ClickX` | 点击 X 坐标，支持多个用逗号分隔 | `300,500,700` |
| `ClickY` | 点击 Y 坐标，支持多个用逗号分隔 | `400,600,800` |
| `ClickButton` | 点击按键，支持多个用逗号分隔 | `L,R,L` |
| `Cooldown` | 触发后冷却时间，单位毫秒 | `2000` |
| `ClickSequenceDelay` | 本区域内点击序列间隔，覆盖全局设置 | `100,500` |
| `Enabled` | 是否启用该区域：`1`=是，`0`=否 | `1` |

---

## 三、热键说明

| 热键 | 功能 |
|------|------|
| `F4` | 显示并复制当前鼠标坐标到剪贴板 |
| `F5` | 重新加载配置文件 |
| `F6` | 用记事本打开配置文件 |
| `F7` | 开始 / 暂停监控 |
| `F8` | 添加新的监控区域（按两次：第一次记录左上角，第二次记录右下角） |
| `F9` | 删除鼠标指针所在的监控区域 |
| `F10` | 开启 / 关闭循环点击 |
| `Ctrl + Shift + S` | 退出脚本 |

---

## 四、点击序列说明

当某个区域满足条件时，脚本会按照 `ClickX`、`ClickY`、`ClickButton` 的顺序依次点击。

- 如果 `ClickX` / `ClickY` 包含多个坐标，将按位置依次点击。
- 如果只有 `ClickButton` 包含多个值，将在同一位置依次按不同按键点击。
- 每两次点击之间的间隔由 `ClickSequenceDelay` 控制。

### ClickSequenceDelay 规则

- 全局默认值在 `[Settings]` 中设置。
- 每个区域可单独设置，覆盖全局值。
- 可配置单个值或多个值：
  - `ClickSequenceDelay=100`：所有间隔均为 100ms。
  - `ClickSequenceDelay=100,500,200`：第 1 个间隔 100ms，第 2 个 500ms，第 3 个 200ms，之后使用最后一个值。

### 示例

```ini
ClickX=1980,1980,1875,1565
ClickY=1034,1034,1188,1196
ClickButton=L,L,L,L
ClickSequenceDelay=100,500,1000
```

表示：
1. 点击 `(1980, 1034)`
2. 等待 100ms
3. 点击 `(1980, 1034)`
4. 等待 500ms
5. 点击 `(1875, 1188)`
6. 等待 1000ms
7. 点击 `(1565, 1196)`

---

## 五、循环点击

循环点击功能会独立于监控循环，在指定坐标反复点击。

- 开关：`F10`
- 相关配置项：`LoopClickEnabled`、`LoopClickX`、`LoopClickY`、`LoopClickInterval`、`LoopClickButton`

---

## 六、常见问题

### 1. OCR 识别不到数字

- 确认监控区域未被遮挡。
- 尝试改用 Tesseract OCR。
- 检查截图是否生成：临时文件路径为 `%TEMP%\ScreenMonitor_ZoneN.png`。
- 查看日志：`ScreenMonitorLog.txt`。

### 2. 截图失败（PowerShell capture failed）

- 脚本会自动回退到 GDI+ 截图。
- 若仍失败，请检查权限或尝试以管理员身份运行。

### 3. 数字带千分位逗号（如 `70,000`）

- 脚本已自动去除逗号后转换数字，无需额外配置。

### 4. 修改配置后未生效

- 修改 `ScreenMonitorConfig.ini` 后，按 `F5` 重新加载配置。

---

## 七、日志

脚本运行日志保存在：`ScreenMonitorLog.txt`，可用于排查识别和点击问题。
